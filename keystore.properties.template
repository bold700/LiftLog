# Keystore credentials voor release signing
# Kopieer dit bestand naar keystore.properties en vul je echte passwords in
# Let op: keystore.properties staat al in .gitignore, dus wordt niet gecommit

storePassword=jouw-keystore-wachtwoord
keyAlias=jouw-key-alias
keyPassword=jouw-key-wachtwoord
